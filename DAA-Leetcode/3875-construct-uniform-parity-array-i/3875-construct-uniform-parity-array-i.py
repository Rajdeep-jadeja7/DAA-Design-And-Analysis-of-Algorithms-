class Solution:
    def uniformArray(self, nums1: list[int]) -> bool:
        return True

        #the output will always be true 
        #because if all the numbers are even then true 
        #and if all numbers are not even then we can make the entire array of odd numbers
        #bcoz even - odd = odd
        # even + odd = odd
        # so output will be always true


       



        